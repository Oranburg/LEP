source "https://rubygems.org"

# Local development only. GitHub Pages builds this site with build_type
# legacy, which ignores this Gemfile and uses GitHub's own Jekyll
# pipeline. Keep this file minimal and matched to local Jekyll 4.x.

gem "jekyll", "~> 4.3"

group :jekyll_plugins do
  gem "jekyll-feed"
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end

gem "webrick", "~> 1.8"
